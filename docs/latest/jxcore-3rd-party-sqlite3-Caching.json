{
  "source": "doc/api/jxcore-3rd-party-sqlite3-Caching.markdown",
  "modules": [
    {
      "textRaw": "Caching",
      "name": "caching",
      "desc": "<p><code>node-sqlite3</code> has a built-in database object cache to avoid opening the same database multiple times. To use the caching feature, simply use <code>new sqlite3.cached.Database()</code> instead of <code>new sqlite3.Database()</code>.\n</p>\n",
      "type": "module",
      "displayName": "Caching"
    }
  ]
}
